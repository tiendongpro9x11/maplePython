
plotsetup(default):
interface(plotdevice):
plotsetup(tek,kermit):
plotsetup(ps,plotoutput=`plot.ps`,plotoptions=`portrait, noborder, width=1000, height=500`):
plotsetup(x11):
plotsetup(maplet):
plot([((1)*x^2+(1)*x+(1))/((2)*x+(1)),1/2*x+1/4],x,color=["Red","Red"]);
